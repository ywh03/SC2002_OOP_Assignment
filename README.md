# Internship Placement Management Application

Clone / Open the Project

Simply open the project folder in your IDE.

Ensure the /data Directory Exists

Inside the project root, ensure you have:
```
data/
    users.ser
    internships.ser
    internshipApplications.ser
```
If these files don’t exist, the system will automatically generate them on first run.

## How to Run
Via IDE
1.	Open MainApp.java
2.	Run the main() method

Via Terminal
```
javac boundary/MainApp.java
java boundary.MainApp
```